# Project Instructions

## Current Documentation

Use these files as the current source of truth:

- `docs/README.md`
- `docs/product-definition.md`
- `docs/course-experience-model.md`
- `docs/technical-operations.md`

Treat `docs/archive/**` as historical reference only.

Do not read, search, cite, or use `docs/archive/**` for current product, architecture, or implementation decisions unless the user explicitly asks for legacy context, provenance, comparison, or recovery.

When searching documentation, exclude `docs/archive/**` by default.

## Agent Operation

Use `vowline` consistently for substantive work.

Do not spawn subagents unless the user explicitly asks for subagents in the current turn.

